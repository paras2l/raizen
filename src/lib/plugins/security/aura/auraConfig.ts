export const auraConfig = {
  scanRangeMeters: 50,
  sensitivityThresholdDb: -110,
  refreshRateMs: 500,
  safeDeviceWhitelist: [], // IDs of user's own devices
  alertThreshold: 0.85, // Confidence level to trigger alert
};
