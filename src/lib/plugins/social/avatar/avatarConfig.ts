import { AvatarConfig as BaseConfig } from './types';

export const avatarConfig = {
  autonomousHuntingEnabled: true,
  minLeadScore: 0.7,
  maxDailyOutreach: 15,
  defaultTone: 'professional-warm',
  platforms: ['linkedin', 'x', 'freelancer', 'upwork'],
  approvalRequired: true
};
